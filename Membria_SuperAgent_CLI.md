# Membria SuperAgent — умный middleware между IDE и код‑агентом

Membria — не IDE, не агент и не “ещё один prompt manager”. Это прослойка между тобой и AI, которая превращает историю твоих решений в структурированные данные и возвращает их агенту до генерации.  
Суть: **не больше контекста, а умнее контекст** — меньше токенов, больше пользы.

---

## Полный цикл

```
Ты пишешь код с агентом
    │
    ├── 1) CAPTURE — фиксация решений из сессии
    │   ├── Engram: полный snapshot (transcript, tool calls, файлы, токены)
    │   ├── Signal Detector: дешёвые паттерны “I chose X” (Level 2)
    │   └── Haiku Extractor: батч‑извлечение сложных решений (Level 3)
    │
    ├── 2) TRACK — решения живут в графе
    │   ├── Decision + связи с Engram и CodeChange
    │   ├── Git trailer: Membria-Engram: eng_xxx в коммитах
    │   └── Outcome Tracker: PR → CI → merge → 30 дней → SUCCESS/FAILURE
    │
    ├── 3) LEARN — из outcome рождается знание
    │   ├── Negative Knowledge: что сломалось и почему
    │   ├── AntiPatterns: повторяющиеся провалы
    │   ├── Calibration Profile: где ты over/under‑confident
    │   └── Skills: green/yellow/red зоны по реальной статистике
    │
    ├── 4) INJECT — возвращаем компактные сигналы до генерации
    │   ├── Calibration Warning (приоритет)
    │   ├── Negative Evidence
    │   ├── AntiPattern Guard
    │   └── Positive Precedent
    │
    └── 5) PROTECT — firewall перед записью
        ├── RedFlag Detector (low confidence, no alternatives, time pressure)
        ├── Bias Detector (anchoring, confirmation, sunk cost)
        └── ALLOW → WARN → BLOCK
```

---

## Почему память решений хранится в графе

Память решений — это не список заметок.  
Это **сеть причинно‑следственных связей**:  
решение → код → результат → следующее решение.

Граф нужен потому что он отвечает на вопросы, которые линейные логи не решают:

- Что именно в прошлом привело к провалу?
- Какие решения в похожем контексте давали лучший outcome?
- Где мы системно переоцениваем себя?
- Какие антипаттерны повторяются не “в теории”, а в нашем опыте?

Граф позволяет делать такие запросы напрямую и возвращать **маленький, но сильный контекст** вместо “всей истории”.

---

## Реальные исходы через вебхуки — что это значит

Решения нельзя оценить в момент принятия — они раскрываются через время.  
Membria фиксирует **объективные события из GitHub/CI**:

- PR создан и замёржен
- CI прошёл или упал
- тесты падали
- инциденты/регрессии
- деградация перфоманса

Эти события превращаются в Outcome.  
То есть ты перестаёшь спорить “кажется, решение было хорошим” — у тебя есть факт, что произошло после него.

---

## Калибровка: что это и откуда статистика

Калибровка — это разница между **уверенностью** и **реальным успехом**.

Как считается статистика:

1. Берём решения по домену (например, `database`)
2. Смотрим исходы (success/failure/partial)
3. Считаем реальный success rate
4. Сравниваем с заявленной уверенностью

Результат:

- если ты стабильно переоцениваешь себя — Membria предупреждает и снижает confidence
- если недооцениваешь — поднимает confidence

Это и есть смысл калибровки: **превратить “ощущения” в цифры**.

---

## Компактные сигналы: зачем они разработчику

Большой контекст = дорого и шумно.  
Membria делает по‑другому: возвращает короткие сигналы, которые реально влияют на решение.

Что именно агент получает:

- 1–3 похожих решения и их исходы
- одно предупреждение о калибровке
- 1–2 антипаттерна из твоей истории

Для разработчика это значит:

- меньше повторных ошибок
- меньше бессмысленных обсуждений
- быстрее принятие решений
- меньше токенов

---

## Что это даёт соло‑вайбкодеру

- Ты один, значит нет командной памяти. Membria заменяет её.
- Ты быстро развиваешь продукт, значит часто ошибаешься на новых развилках. Membria фиксирует это.
- У тебя нет code review и тимлида. Membria создаёт “память решений” и “контроль последствий”.

Итог: агент не “становится умнее” сам по себе, но получает твой опыт в виде компактного контекста — и качество решений растёт.

---

---

## Текущий статус реализации (MVP — Phase 3 Complete)

Мы завершили основные этапы разработки интерактивного ядра Membria CLI:

1.  **🚀 Shell Infrastructure (Phase 1)**: Полноценный REPL на базе `prompt_toolkit`, onboarding-визард для провайдеров (Anthropic, OpenAI, Ollama) и система настройки команды агентов.
2.  **🧠 Intelligent Delegation (Phase 2)**: Интеграция концепций Google DeepMind.
    - **Reputation Ledger**: Статистика агентов хранится в FalkorDB.
    - **Liability Firebreaks**: Система безопасности, блокирующая необратимые действия (delete/deploy) до подтверждения человеком.
    - **Monitoring Levels (L0-L3)**: Настраиваемая глубина логирования от простых уведомлений до полных трейсов рассуждений.
3.  **🎭 Orchestration Modes (Phase 3)**:
    - **Auto-Routing**: Автоматический выбор режима (Pipeline/Parallel/Single) на основе сложности задачи.
    - **Context Injection (RAG)**: Прямая интеграция с графом — агенты видят историю похожих решений и их исходы до начала работы.
    - **Consensus Flow**: Параллельная генерация решений разными агентами с последующим синтезом лучшего варианта.

## Что дальше (Phase 4 & Beyond)

1.  **🔌 IDE Integration**: Связка CLI-агентов с расширением VSCode для контекста в реальном времени.
2.  **🖇 Git Hooks Capture**: Автоматическая фиксация решений при коммитах и пушах без ручного ввода.
3.  **🛡 Advanced Guardrails**: Глубокий анализ "Red Flags" (anchoring bias, sunk cost) через специализированных агентов-рецензентов.
4.  **📊 Analytics Dashboard**: Визуализация графа решений и калибровки через браузерный интерфейс.
