# Membria Interactive CLI — Design Spec

**Концепция:** Membria CLI — это не просто утилита, а **интерактивная оболочка для оркестрации нескольких code LLM**. Как Claude Code CLI, но с возможностью подключать и управлять несколькими AI одновременно через ролевую модель.

---

## Запуск: ANSI Splash Screen

```
$ membria

 ███╗   ███╗███████╗███╗   ███╗██████╗ ██████╗ ██╗ █████╗ 
 ████╗ ████║██╔════╝████╗ ████║██╔══██╗██╔══██╗██║██╔══██╗
 ██╔████╔██║█████╗  ██╔████╔██║██████╔╝██████╔╝██║███████║
 ██║╚██╔╝██║██╔══╝  ██║╚██╔╝██║██╔══██╗██╔══██╗██║██╔══██║
 ██║ ╚═╝ ██║███████╗██║ ╚═╝ ██║██████╔╝██║  ██║██║██║  ██║
 ╚═╝     ╚═╝╚══════╝╚═╝     ╚═╝╚═════╝ ╚═╝  ╚═╝╚═╝╚═╝  ╚═╝
                                                    v1.0.0
              Decision Intelligence SuperAgent

 ┌─────────────────────────────────────────────────────┐
 │  🧠 Graph:    ✅ FalkorDB (192.168.0.105:6379)      │
 │  📊 Decisions: 47 captured, 31 with outcomes        │
 │  ⚡ Calibration: -8% overconfident (improving)      │
 │                                                     │
 │  🤖 Agents:                                         │
 │     ├── 🏗️  Claude Sonnet  [Architect]    ✅ Ready  │
 │     ├── ⚡ Codex GPT-4.1   [Implementer]  ✅ Ready  │
 │     └── 🔍 KiloCode GLM-5  [Reviewer]     ✅ Ready  │
 │                                                     │
 │  📁 Project: ~/myapp (Node.js + TypeScript)         │
 │  🔥 Last session: 2h ago (12 decisions, 3 plans)    │
 └─────────────────────────────────────────────────────┘

 Type a task, or /help for commands.

 membria ~/myapp ▸ 
```

---

## Интерактивный Prompt

### Формат prompt

```
membria ~/project [agents: 3] ▸ 
```

С контекстом активной задачи:

```
membria ~/myapp [rate-limiter] 🏗️ Claude planning ▸ 
```

### Ввод задач на естественном языке

```
membria ~/myapp ▸ Добавить rate limiting для API

🏗️ Architect (Claude) анализирует задачу...

┌─ 🧠 DECISION CONTEXT ──────────────────────────────────┐
│                                                         │
│ 📋 Похожие решения:                                     │
│    ✅ express-rate-limit (6 мес назад) → SUCCESS         │
│    ❌ Custom rate limiter (1 год назад) → FAILURE        │
│                                                         │
│ ⚠️  NegativeKnowledge:                                  │
│    "Custom rate limiting removed in 78% of repos"       │
│                                                         │
│ 📊 Калибровка (infra):                                  │
│    Команда точна: gap +3%, sample: 24                   │
│                                                         │
└─────────────────────────────────────────────────────────┘

🏗️ Architect предлагает:

  Decision: "Use express-rate-limit + Redis store"
  Alternatives: Custom middleware, Nginx, API Gateway
  Confidence: 0.85 (calibrated: 0.82)
  
  Plan (5 steps):
   1. Install express-rate-limit + rate-limit-redis
   2. Create src/middleware/rateLimiter.ts
   3. Configure per-endpoint limits
   4. Add Redis connection for distributed state
   5. Integration tests
   
  Estimated: 25 min

  [y] Принять  [e] Редактировать  [a] Альтернативы  [?] Спросить

▸ y

✅ Decision captured: dec_048
⚡ Отправляю реализацию...

┌─ ⚡ IMPLEMENTATION ─────────────────────────────────────┐
│                                                         │
│ Codex (GPT-4.1) реализует шаги 1-4 параллельно...      │
│                                                         │
│ Step 1: npm install          ████████████████████ Done   │
│ Step 2: rateLimiter.ts       ██████████████░░░░░░ 70%   │
│ Step 3: endpoint config      ██████████░░░░░░░░░░ 50%   │
│ Step 4: Redis connection     ████████████████████ Done   │
│ Step 5: tests                ░░░░░░░░░░░░░░░░░░░ Queue  │
│                                                         │
│ Files: 3 created, 1 modified                            │
└─────────────────────────────────────────────────────────┘

⚡ Codex завершил реализацию (4/5 шагов)
🔍 KiloCode (GLM-5) проверяет код...

┌─ 🔍 REVIEW ────────────────────────────────────────────┐
│                                                         │
│ ✅ Rate limiter implementation correct                   │
│ ✅ Redis connection follows project patterns             │
│ ⚠️  Missing: Rate limit headers in response              │
│ ⚠️  Suggestion: Add retry-after header                   │
│ 📝 Generated 8 tests, 7 passing, 1 needs fix            │
│                                                         │
└─────────────────────────────────────────────────────────┘

  [y] Применить всё  [f] Показать файлы  [d] Diff  [r] Исправить

▸ d
```

---

## Ролевая Модель: 3 Агента

```
┌───────────────────────────────────────────────────────────┐
│                    Membria (TeamLead)                      │
│                                                           │
│  Контролирует процесс:                                    │
│  • Routing задач к агентам                                │
│  • Применение NegativeKnowledge                           │
│  • Калибровка уверенности                                 │
│  • Проверка constraints                                   │
│  • Запись решений/outcomes в граф                          │
│                                                           │
│     ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│     │ 🏗️       │    │ ⚡       │    │ 🔍       │         │
│     │Architect │    │Implement.│    │ Reviewer  │         │
│     │(Claude)  │    │(Codex)   │    │(KiloCode) │         │
│     │          │    │          │    │           │         │
│     │Planning  │    │Coding    │    │Testing    │         │
│     │Design    │    │Refactor  │    │Review     │         │
│     │Decisions │    │Boilerplt │    │Edge cases │         │
│     │Docs      │    │CRUD      │    │Alt. views │         │
│     └──────────┘    └──────────┘    └──────────┘         │
│                                                           │
│     Очерёдность: Architect → Implementer → Reviewer       │
│     Или параллельно — зависит от задачи                   │
└───────────────────────────────────────────────────────────┘
```

### Membria = TeamLead

Membria CLI **не просто обёртка** над LLM. Она играет роль TeamLead:

```
Что делает TeamLead (Membria):
├── Принимает задачу от разработчика
├── Проверяет граф на похожие решения
├── Показывает NegativeKnowledge ДО того как AI начнёт работу
├── Назначает задачу нужному агенту (или нескольким)
├── Калибрует уверенность: "AI говорит 85% → реально ~72%"
├── Контролирует: "Codex предложил custom auth → БЛОК"
├── Записывает всё в граф для будущих решений
└── Учится: outcome → обновляет калибровку → становится точнее
```

---

## Slash-команды

```
/help                  — Справка по командам
/status                — Статус системы и агентов
/agents                — Управление подключёнными LLM
/agents add claude     — Подключить Claude
/agents remove codex   — Отключить Codex
/agents list           — Список с калибровкой

/mode pipeline         — Режим: Architect → Implement → Review
/mode fanout           — Режим: все параллельно, выбрать лучший
/mode specialist       — Режим: каждому свой домен
/mode solo claude      — Работать только с Claude

/context               — Показать текущий контекст из графа
/decisions             — Последние решения
/calibration           — Калибровка команды

/plan                  — Создать план
/validate              — Проверить текущий план
/outcome <id> success  — Записать результат

/diff                  — Показать изменения
/files                 — Показать созданные/изменённые файлы
/apply                 — Применить изменения в проект
/undo                  — Откатить последнее изменение

/cost                  — Стоимость сессии (токены по модели)
/session               — Статистика сессии
/save                  — Сохранить сессию
/exit                  — Выход
```

### Быстрый доступ к агентам

```
@architect  Спроектируй API для уведомлений
@coder      Реализуй этот интерфейс
@reviewer   Проверь последний коммит

# Или прямое обращение:
@claude     Объясни архитектуру этого модуля
@codex      Сгенерируй CRUD для users
@kilocode   Напиши тесты для rate limiter
```

---

## Сессия: Полный Пример

```
$ membria

 ███╗   ███╗███████╗███╗   ███╗██████╗ ██████╗ ██╗ █████╗ 
 ...
 🤖 Agents: Claude ✅  Codex ✅  KiloCode ✅

membria ~/myapp ▸ Нужна система уведомлений: email, push, in-app

🧠 Membria (TeamLead):
   Проверяю граф... Найдено:
   ✅ SendGrid для email (dec_023) → SUCCESS
   ❌ Custom email sender (dec_009) → FAILURE  
   ⚠️  Push notifications — нет данных (первый раз)

   Рекомендация: используй проверенный подход для email,
   запрос дополнительного review для push (нет истории)

   Режим: Pipeline (Architect → 2 Implementers → Reviewer)

🏗️ @architect Спроектируй систему уведомлений...

   Claude разработал архитектуру:
   
   NotificationService
   ├── EmailChannel (SendGrid)
   ├── PushChannel (Firebase FCM)  
   ├── InAppChannel (WebSocket)
   └── UserPreferences (PostgreSQL)
   
   Файлы: 8 новых, 2 модификации
   Estimated: 35 min
   
   [y] Принять  [e] Редактировать план

▸ y

✅ Plan captured. Распределяю задачи...

┌─ TASK DISTRIBUTION ──────────────────────────────────────┐
│                                                          │
│ ⚡ Codex:                                                │
│   ├── NotificationService (core)        🔄 working       │
│   ├── EmailChannel (SendGrid)           ⏳ queued        │
│   └── UserPreferences CRUD             ⏳ queued        │
│                                                          │
│ 🔍 KiloCode:                                             │
│   ├── PushChannel (Firebase)            🔄 working       │
│   ├── InAppChannel (WebSocket)          ⏳ queued        │
│   └── Tests (all channels)             ⏳ queued        │
│                                                          │
│ Progress: ██████░░░░░░░░░░░░░░ 30%  ETA: 4 min          │
└──────────────────────────────────────────────────────────┘

# Разработчик может параллельно спрашивать:
▸ @architect Почему Firebase а не OneSignal?

🏗️ Claude: "Firebase FCM выбран потому что:
   1. У вас уже Firebase Auth (dec_031)
   2. Единая экосистема снижает сложность
   3. OneSignal — extra dependency без преимущества
   Калибровка: решения "единая экосистема" success rate 82%"

# Задачи продолжают выполняться в фоне...

┌─ TASK DISTRIBUTION ──────────────────────────────────────┐
│ ⚡ Codex:    3/3 done ✅                                  │
│ 🔍 KiloCode: 2/3 done, tests running...                  │
│ Progress: ████████████████████ 90%  ETA: 1 min            │
└──────────────────────────────────────────────────────────┘

✅ Все задачи завершены!

🔍 KiloCode Review:
   ✅ 12 tests passing
   ⚠️  1 issue: PushChannel нет retry при FCM timeout
   📝 Fix: добавлен exponential backoff (auto-applied)

📦 Результат:
   Created:  8 files
   Modified: 2 files
   Tests:    12 passing, 0 failing

   [y] Применить  [d] Diff  [f] Файлы  [r] Отклонить

▸ y

✅ Изменения применены к ~/myapp
✅ Decision dec_049 recorded (notification system)
📊 Outcome отслеживается — проверка через 30 дней

membria ~/myapp ▸ 
```

---

## Технический Стек CLI

| Компонент | Библиотека | Назначение |
|-----------|-----------|-----------|
| CLI framework | `typer` + `click` | Команды и парсинг |
| Rich UI | `rich` | ANSI графика, таблицы, progress bars |
| REPL | `prompt_toolkit` | Интерактивный ввод, autocomplete, history |
| Async | `asyncio` | Параллельная работа агентов |
| Streaming | `httpx` + `aiohttp` | SSE/streaming от LLM API |
| Файловая система | `watchdog` | Отслеживание изменений |

### Архитектура CLI

```python
# Концепт ключевых классов:

class MembriaShell:
    """Главный интерактивный shell"""
    
    def __init__(self):
        self.graph = GraphClient()           # FalkorDB
        self.router = TaskRouter()           # Классификация задач
        self.agents = AgentRegistry()        # Подключённые LLM
        self.session = Session()             # История сессии
        self.teamlead = TeamLeadAgent()      # Мозг оркестрации
    
    async def run(self):
        """Главный REPL loop"""
        self.show_splash()
        self.show_status()
        
        while True:
            user_input = await self.prompt()
            
            if user_input.startswith('/'):
                await self.handle_command(user_input)
            elif user_input.startswith('@'):
                await self.handle_direct_agent(user_input)
            else:
                await self.handle_task(user_input)
    
    async def handle_task(self, task: str):
        """Полный pipeline обработки задачи"""
        # 1. TeamLead проверяет контекст
        context = self.teamlead.check_context(task)
        self.display_context(context)
        
        # 2. Architect планирует
        plan = await self.agents.architect.plan(task, context)
        self.display_plan(plan)
        
        # 3. Ждём подтверждения
        if not await self.confirm(plan):
            return
        
        # 4. Распределяем и запускаем параллельно
        tasks = self.router.split(plan)
        results = await self.agents.execute_parallel(tasks)
        
        # 5. Reviewer проверяет
        review = await self.agents.reviewer.review(results)
        self.display_review(review)
        
        # 6. Применяем
        if await self.confirm_apply(results):
            self.apply_to_workspace(results)
            self.graph.record_decision(plan)


class AgentRegistry:
    """Реестр подключённых LLM агентов"""
    
    agents: Dict[str, Agent] = {}
    
    def add(self, name: str, provider: str, role: str, api_key: str):
        """Подключить нового агента"""
        # provider: "anthropic", "openai", "zhipu" (GLM)
        # role: "architect", "implementer", "reviewer"
    
    async def execute_parallel(self, tasks: List[Task]) -> List[Result]:
        """Запустить задачи параллельно на разных агентах"""
        return await asyncio.gather(*[
            self.agents[t.assigned_to].execute(t)
            for t in tasks
        ])


class TeamLeadAgent:
    """Membria как TeamLead — оркестрация без LLM"""
    
    def check_context(self, task: str) -> Context:
        """Проверить граф, NegKnowledge, калибровку"""
    
    def should_block(self, proposal: str) -> Optional[str]:
        """Заблокировать если предложение нарушает constraints"""
    
    def calibrate(self, confidence: float, domain: str) -> float:
        """Скорректировать уверенность по данным калибровки"""
```

---

## Маркетплейс Моделей: Гибкая Конфигурация

Ключевая идея: **провайдеры и модели — отдельно, роли — отдельно**. Ты подключаешь провайдеров (Claude, OpenAI, KiloCode, Ollama, и т.д.), видишь все доступные модели, и назначаешь их на роли в команде. Каждый провайдер открывает доступ к моделям, которых нет у других.

### Что даёт каждый провайдер

```
┌─ Anthropic ────────────────────────────────────────────────┐
│  claude-sonnet-4     — лучший для архитектуры и планов     │
│  claude-haiku-3.5    — быстрый, дешёвый, для мелких задач  │
│  claude-opus-4       — самый умный, для критичных решений  │
└────────────────────────────────────────────────────────────┘

┌─ OpenAI ───────────────────────────────────────────────────┐
│  gpt-4.1             — сильный кодер, хорош в рефакторинге │
│  gpt-4.1-mini        — быстрее, дешевле, для CRUD          │
│  o3                  — лучший в reasoning и дебаге          │
│  o4-mini             — reasoning, но дешевле                │
│  codex-mini          — специализирован на коде              │
└────────────────────────────────────────────────────────────┘

┌─ KiloCode ─────────────────────────────────────────────────┐
│  glm-5               — ZhipuAI, сильный в азиатских стеках │
│  deepseek-v3         — отличный кодер, низкая цена         │
│  deepseek-r1         — reasoning, альтернатива o3           │
│  qwen-2.5-coder      — Alibaba, хорош в коде               │
│  qwen-max            — general purpose                      │
│  mistral-large       — европейская модель                   │
│  codestral           — Mistral, специализация на коде       │
└────────────────────────────────────────────────────────────┘

┌─ Ollama (локальные) ───────────────────────────────────────┐
│  llama-3.3-70b       — локально, без API costs              │
│  codellama-34b       — локальный кодер                      │
│  deepseek-coder-33b  — локальный DeepSeek                   │
│  qwen-2.5-coder-32b  — локальный Qwen                      │
└────────────────────────────────────────────────────────────┘
```

### Конфигурация: Провайдеры + Команда

```yaml
# ~/.membria/config.yaml

# ═══ ПРОВАЙДЕРЫ (подключённые API) ═══

providers:
  anthropic:
    api_key_env: ANTHROPIC_API_KEY
    
  openai:
    api_key_env: OPENAI_API_KEY
    
  kilocode:
    # KiloCode даёт доступ через свой прокси
    api_key_env: KILOCODE_API_KEY
    endpoint: https://api.kilocode.ai/v1
    
  ollama:
    endpoint: http://localhost:11434
    # Без API key — локальные модели

# ═══ КОМАНДА (роли и назначенные модели) ═══

team:
  architect:
    provider: anthropic
    model: claude-sonnet-4
    label: "🏗️ Architect"
    strengths: [planning, architecture, decisions, documentation]
    
  senior:
    provider: openai
    model: gpt-4.1
    label: "⚡ Senior Dev"
    strengths: [implementation, refactoring, complex-logic]
    
  junior:
    provider: kilocode
    model: deepseek-v3
    label: "💻 Junior Dev"
    strengths: [boilerplate, crud, simple-tasks]
    cost_priority: true     # предпочитать этого для дешёвых задач
    
  reviewer:
    provider: kilocode
    model: qwen-2.5-coder
    label: "🔍 Reviewer"
    strengths: [testing, code-review, edge-cases]
    
  debugger:
    provider: openai
    model: o3
    label: "🐛 Debugger"
    strengths: [debugging, reasoning, root-cause-analysis]
    on_demand: true         # не запускать по умолчанию

# ═══ ОРКЕСТРАЦИЯ ═══

orchestration:
  default_mode: pipeline
  auto_apply: false
  max_parallel: 4
  timeout: 120
  cost_limit: 5.00         # лимит на сессию в $
  prefer_cheap: true        # использовать дешёвые модели для мелких задач
```

### Примеры команд-составов

**Команда "Экономная" (минимум затрат):**
```yaml
team:
  architect:
    provider: kilocode
    model: deepseek-r1        # reasoning, дёшево
  coder:
    provider: kilocode
    model: deepseek-v3        # быстрый кодер, дёшево
  reviewer:
    provider: ollama
    model: llama-3.3-70b      # бесплатно (локально)
```

**Команда "Максимум качества":**
```yaml
team:
  architect:
    provider: anthropic
    model: claude-opus-4       # самый умный
  senior:
    provider: openai
    model: gpt-4.1             # сильный кодер
  junior:
    provider: anthropic
    model: claude-haiku-3.5    # быстрый для мелочей
  reviewer:
    provider: openai
    model: o3                  # глубокий анализ
```

**Команда "Мультикультурная" (разные перспективы):**
```yaml
team:
  architect:
    provider: anthropic
    model: claude-sonnet-4     # западный подход
  coder_a:
    provider: openai
    model: gpt-4.1             # OpenAI стиль
  coder_b:
    provider: kilocode
    model: deepseek-v3         # китайский подход
  reviewer:
    provider: kilocode
    model: codestral           # европейский (Mistral)
```

### Управление командой в runtime

```
membria ~/myapp ▸ /agents

┌─ 🤖 ACTIVE AGENTS ───────────────────────────────────────┐
│                                                           │
│ Role        Model              Provider   Cost/1K  Cal.   │
│ ─────────── ────────────────── ────────── ─────── ─────── │
│ 🏗️ Architect claude-sonnet-4   Anthropic  $3/$15  89% ✅  │
│ ⚡ Senior    gpt-4.1           OpenAI     $2/$8   76% ✅  │
│ 💻 Junior    deepseek-v3       KiloCode   $0.1/$0.3 71% ✅│
│ 🔍 Reviewer  qwen-2.5-coder   KiloCode   $0.5/$1  68% ✅ │
│ 🐛 Debugger  o3               OpenAI     $10/$40  — 💤   │
│                                                           │
│ Session cost: $0.42  Decisions: 3  Tokens: 12.4K          │
└───────────────────────────────────────────────────────────┘

membria ~/myapp ▸ /agents swap junior ollama/codellama-34b

✅ Junior Dev changed: deepseek-v3 → codellama-34b (Ollama)
   Note: калибровка сброшена — нет данных по этой модели

membria ~/myapp ▸ /agents add qa kilocode/mistral-large --role reviewer

✅ Added QA agent (mistral-large via KiloCode)
   Role: Reviewer | Strengths: auto-detected from model

membria ~/myapp ▸ /agents cost

┌─ 💰 SESSION COST BREAKDOWN ──────────────────────────────┐
│                                                           │
│ 🏗️ Architect (claude-sonnet-4):  $0.28 (4.2K tokens)     │
│ ⚡ Senior (gpt-4.1):             $0.11 (3.1K tokens)     │
│ 💻 Junior (deepseek-v3):         $0.02 (5.8K tokens)     │
│ 🔍 Reviewer (qwen-2.5-coder):   $0.01 (2.3K tokens)     │
│                                                           │
│ Total: $0.42 / $5.00 limit                                │
│ ████░░░░░░░░░░░░░░░░ 8%                                   │
└───────────────────────────────────────────────────────────┘
```

### Автоматическое назначение моделей

Membria (TeamLead) может **автоматически выбирать модель** под задачу:

```
membria ~/myapp ▸ Исправь баг в auth middleware

🧠 TeamLead: Задача = debugging + auth
   Auto-routing:
   • Auth — домен с calibration gap +12%
   • Debugging — нужен reasoning
   → Активирую 🐛 Debugger (o3) вместо Senior
   → Причина: o3 лучше в root-cause analysis

🐛 Debugger (o3) анализирует...
```

```
membria ~/myapp ▸ Добавь CRUD для заметок

🧠 TeamLead: Задача = boilerplate CRUD
   Auto-routing:
   → 💻 Junior (deepseek-v3) — $0.01 вместо $0.30
   → Причина: простая задача, не стоит тратить на Senior

💻 Junior (deepseek-v3) генерирует...
```

---

## Отличие от Claude Code / Cursor / Codex CLI

| Фича | Claude Code | Cursor | **Membria CLI** |
|-------|:-----------:|:------:|:---------------:|
| Один LLM | ✅ | ✅ | ✅ |
| Несколько LLM параллельно | ❌ | ❌ | **✅ 3+ одновременно** |
| Выбор любой модели | ❌ (только Claude) | Частично | **✅ Любой провайдер** |
| Микс провайдеров | ❌ | ❌ | **✅ Claude+OpenAI+KiloCode+Ollama** |
| Ролевая команда | ❌ | ❌ | **✅ Architect/Senior/Junior/Reviewer** |
| Decision memory | ❌ | ❌ | **✅ FalkorDB граф** |
| Калибровка per-model | ❌ | ❌ | **✅ Какая модель лучше в чём** |
| NegativeKnowledge | ❌ | ❌ | **✅ Блокирует ошибки** |
| Cost optimization | ❌ | ❌ | **✅ Дешёвая модель для мелочей** |
| Auto-routing | ❌ | ❌ | **✅ Модель под задачу** |
| Оркестрация | ❌ | ❌ | **✅ Pipeline/FanOut/Specialist** |
| Outcome tracking | ❌ | ❌ | **✅ 30-day мониторинг** |
| TeamLead agent | ❌ | ❌ | **✅ Membria сама** |
| Локальные модели | ❌ | ❌ | **✅ Ollama integration** |
