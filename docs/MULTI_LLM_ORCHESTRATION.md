# Multi-LLM Orchestration через Membria

**Кейс:** Разработчик работает в VSCode. Через Membria к рабочему процессу подключены три code LLM, работающие параллельно. Каждый получает задачи по специализации, все делят общий граф знаний.

---

## Архитектура: VSCode как пульт управления

```
┌─────────────────────────────────────────────────────────────┐
│                        VSCode                                │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ Membria      │  │ Membria      │  │   Редактор       │   │
│  │ Sidebar      │  │ Task Queue   │  │   кода           │   │
│  │ (Decisions,  │  │ Panel        │  │                   │   │
│  │  Calibration)│  │ (кто что     │  │                   │   │
│  │              │  │  делает)     │  │                   │   │
│  └──────────────┘  └──────┬───────┘  └──────────────────┘   │
│                           │                                  │
└───────────────────────────┼──────────────────────────────────┘
                            │
                  ┌─────────┴──────────┐
                  │   Membria Router    │
                  │   (Task Splitter +  │
                  │    LLM Selector)    │
                  └──┬──────┬──────┬───┘
                     │      │      │
           ┌─────────┘      │      └─────────┐
           ▼                ▼                ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │   Claude     │ │   Codex      │ │  KiloCode    │
    │   Sonnet     │ │   (GPT-4.1)  │ │  (GLM-5)     │
    │              │ │              │ │              │
    │ Архитектура  │ │ Реализация   │ │ Ревью +      │
    │ Планирование │ │ Рефакторинг  │ │ Альтернативы │
    │ Документация │ │ Boilerplate  │ │ Тесты        │
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           └────────────────┼────────────────┘
                            ▼
                  ┌──────────────────┐
                  │    FalkorDB      │
                  │  (общий граф)    │
                  └──────────────────┘
```

---

## 3 модели работы

### Модель 1: Pipeline (Конвейер)

**Claude планирует → Codex реализует → KiloCode ревьюит**

Лучше всего для: **новая фича с нуля**

```
Разработчик: "Добавить систему rate limiting для API"

═══ ШАГ 1: Claude (Архитектор) ═══════════════════════════════
Задание: Спроектировать rate limiting

Claude видит контекст из графа:
  ✅ Redis работает в проекте (решение 3 месяца назад)
  ❌ Custom rate limiter провалился (NegativeKnowledge)
  📊 Команда точна в infra-решениях (calibration gap: +3%)

Claude выдаёт:
  → План: 5 шагов
  → Решение: "express-rate-limit + Redis store"
  → Структура файлов
  → API контракты

  capture_decision("Use express-rate-limit + Redis", 
    alternatives=["Custom middleware", "Nginx rate limit", "API Gateway"],
    confidence=0.85)

═══ ШАГ 2: Codex (Реализатор) ═══════════════════════════════
Задание: Реализовать план Claude

Codex получает:
  → План Claude (из графа)
  → Constraints: "НЕ использовать custom middleware"
  → Структуру файлов и API контракты
  
Codex генерирует:
  → src/middleware/rateLimiter.ts
  → src/config/rateLimitConfig.ts
  → Обновление src/app.ts

═══ ШАГ 3: KiloCode/GLM5 (Ревьюер) ══════════════════════════
Задание: Проверить реализацию Codex

KiloCode получает:
  → Код от Codex
  → План от Claude
  → NegativeKnowledge из графа

KiloCode выполняет:
  → validate_plan(codex_implementation)
  → Генерирует тесты
  → Находит edge cases
  → Предлагает улучшения

Результат: "2 предупреждения, 1 баг в edge case (race condition)"
```

**Выигрыш:** Каждый LLM работает в своей сильной зоне. Claude не пишет boilerplate, Codex не архитектурит, KiloCode даёт свежий взгляд.

---

### Модель 2: Fan-Out (Параллельное исполнение)

**Одна задача → 3 LLM одновременно → выбрать лучший результат**

Лучше всего для: **критические архитектурные решения**

```
Разработчик: "Как реализовать кэширование для каталога товаров?"

═══ ПАРАЛЛЕЛЬНО: все 3 LLM получают задачу ════════════════════

Membria Router отправляет одну и ту же задачу всем трём:

┌────────────────────────────────────────────────────────┐
│                    Claude (Sonnet)                      │
│                                                        │
│  "Многоуровневый кэш:                                 │
│   L1: In-memory (node-cache) — 5 минут TTL             │
│   L2: Redis — 30 минут TTL                             │
│   Invalidation: Event-driven через pub/sub"            │
│                                                        │
│  Confidence: 0.82                                      │
│  Плюсы: гибкость, fallback                             │
│  Минусы: сложность инвалидации                         │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                    Codex (GPT-4.1)                      │
│                                                        │
│  "Redis-only кэш:                                      │
│   Один уровень, Redis с TTL 15 минут                   │
│   Invalidation: cache-aside pattern"                   │
│                                                        │
│  Confidence: 0.75                                      │
│  Плюсы: простота                                       │
│  Минусы: один point of failure                         │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                   KiloCode (GLM-5)                      │
│                                                        │
│  "CDN + Redis:                                         │
│   Static: CDN edge кэш (Cloudflare)                    │
│   Dynamic: Redis с tag-based invalidation              │
│   Warm-up: Cron pre-population каждые 10 минут"        │
│                                                        │
│  Confidence: 0.70                                      │
│  Плюсы: скорость доставки                              │
│  Минусы: усложняет деплой                              │
└────────────────────────────────────────────────────────┘

═══ MEMBRIA СРАВНИВАЕТ ═══════════════════════════════════════

Membria показывает в VSCode sidebar:

┌──────────────────────────────────────────────────────┐
│ 🧠 Membria: 3 варианта кэширования                   │
│                                                      │
│ 📊 Калибровка по домену "cache":                     │
│   Claude:   90% success rate (12 решений)            │
│   Codex:    65% success rate (8 решений)             │
│   KiloCode: 73% success rate (5 решений)             │
│                                                      │
│ ⚡ Рекомендация: Claude (наивысший success в cache)   │
│                                                      │
│ ⚠️ NegativeKnowledge: "Один уровень кэша            │
│    приводил к cache stampede 2 раза"                  │
│    → Вариант Codex имеет этот риск                   │
│                                                      │
│ [ Взять Claude ] [ Взять KiloCode ] [ Гибрид ]       │
└──────────────────────────────────────────────────────┘

Разработчик выбирает "Гибрид" →
Membria: capture_decision("Многоуровневый кэш Claude + CDN от KiloCode")
```

**Выигрыш:** Разработчик видит 3 перспективы + объективные данные (success rate), а не выбирает вслепую.

---

### Модель 3: Specialist Routing (Маршрутизация по специализации)

**Большая задача → разбить на домены → каждому LLM свой домен**

Лучше всего для: **полноценная фича с несколькими компонентами**

```
Разработчик: "Реализовать систему уведомлений: 
  email, push, in-app + настройки пользователя"

═══ MEMBRIA ROUTER: РАЗБИВКА ═════════════════════════════════

Membria анализирует задачу и разбивает на 4 подзадачи:

  ┌─────────────────────────────────────────────┐
  │ Подзадача 1: Архитектура + API контракты    │ → Claude
  │ Подзадача 2: Email + Push реализация        │ → Codex
  │ Подзадача 3: In-app notifications UI        │ → KiloCode
  │ Подзадача 4: User preferences CRUD          │ → Codex
  └─────────────────────────────────────────────┘

Распределение основано на калибровке:
  Claude: лучший в архитектуре (92% success)
  Codex:  лучший в CRUD/boilerplate (88% success)
  KiloCode: хорош в UI компонентах (79% success)

═══ ПАРАЛЛЕЛЬНАЯ РАБОТА ══════════════════════════════════════

Claude (Подзадача 1):                 ВРЕМЯ
  → Проектирует NotificationService   ████░░░░  2 мин
  → API: POST /notify, GET /prefs     
  → Event schema                      
  → capture_decision(...)             

Codex (Подзадача 2+4):                       
  → Ждёт API контракты от Claude      ░░░░░░░░  ожидание
  → Реализует EmailSender             ░░░░████  3 мин
  → Реализует PushSender              
  → Реализует UserPreferences CRUD    

KiloCode (Подзадача 3):
  → Ждёт API контракты от Claude      ░░░░░░░░  ожидание
  → NotificationBell component        ░░░░████  3 мин
  → NotificationList component        
  → Тесты для UI                      

═══ TIMELINE ═════════════════════════════════════════════════

Минута 0-2:   Claude проектирует (остальные ждут)
Минута 2-5:   Codex + KiloCode работают ПАРАЛЛЕЛЬНО
Минута 5-6:   Membria собирает результаты
Минута 6-7:   Разработчик ревьюит в VSCode

Итого: 7 минут вместо ~20 минут последовательно
```

---

## Практическая реализация в VSCode

### Task Queue Panel (новый view)

```
┌─────────────────────────────────────────────┐
│ 📋 Membria Task Queue                       │
│                                             │
│ Feature: Notification System                │
│ Mode: Specialist Routing                    │
│ Progress: ██████░░░░ 60%                    │
│                                             │
│ 🤖 Claude (Architect)          ✅ Done      │
│    └─ API Design + Schema                   │
│                                             │
│ 🤖 Codex (Implementer)         🔄 Working  │
│    ├─ EmailSender              ✅           │
│    ├─ PushSender               🔄 working  │
│    └─ UserPreferences CRUD     ⏳ queued    │
│                                             │
│ 🤖 KiloCode (UI + Tests)       🔄 Working  │
│    ├─ NotificationBell         ✅           │
│    ├─ NotificationList         🔄 working  │
│    └─ UI Tests                 ⏳ queued    │
│                                             │
│ [ Pause All ] [ Cancel ] [ View Results ]   │
└─────────────────────────────────────────────┘
```

### Results Merge View

После завершения — разработчик видит:

```
┌─────────────────────────────────────────────┐
│ 📦 Merge Results                            │
│                                             │
│ Claude (Architecture):                      │
│   📄 docs/notification-api.md         [✓]   │
│   📄 src/types/notification.ts        [✓]   │
│                                             │
│ Codex (Implementation):                     │
│   📄 src/services/emailSender.ts      [✓]   │
│   📄 src/services/pushSender.ts       [!]   │
│      ⚠️ Conflicт с KiloCode в imports       │
│   📄 src/api/preferences.ts           [✓]   │
│                                             │
│ KiloCode (UI + Tests):                      │
│   📄 src/components/NotifBell.tsx     [✓]   │
│   📄 src/components/NotifList.tsx     [✓]   │
│   📄 tests/notifications.test.ts      [✓]   │
│                                             │
│ Конфликты: 1 (авторазрешимый)               │
│ [ Auto-Merge ] [ Manual Review ]            │
└─────────────────────────────────────────────┘
```

---

## Таблица: Когда какую модель использовать

| Ситуация | Модель | Почему |
|----------|--------|--------|
| Новая фича с нуля | **Pipeline** | Архитектура → код → ревью |
| Критичное решение | **Fan-Out** | 3 мнения + данные калибровки |
| Большая фича (5+ файлов) | **Specialist** | Параллелизм, скорость |
| Баг-фикс | **Один LLM** | Не нужна оркестрация |
| Рефакторинг | **Pipeline** | Claude планирует, Codex рефакторит |
| Code Review | **Fan-Out** | Разные LLM ловят разные проблемы |

---

## Что нужно реализовать

| Компонент | Статус | Сложность |
|-----------|:------:|:---------:|
| MCP Server (базовый) | ✅ Есть | — |
| FalkorDB Schema | ✅ Есть | — |
| LLM Registry (регистрация провайдеров) | ❌ Нет | Средняя |
| Task Router (классификация задач) | ❌ Нет | Высокая |
| Task Splitter (разбивка на подзадачи) | ❌ Нет | Высокая |
| LLM Selector (выбор по калибровке) | ❌ Нет | Средняя |
| Parallel Executor | ❌ Нет | Высокая |
| Results Merger | ❌ Нет | Высокая |
| VSCode Task Queue Panel | ❌ Нет | Средняя |
| VSCode Merge View | ❌ Нет | Средняя |
| Per-LLM Calibration tracking | ❌ Нет | Низкая |

**Из этого уже заложено в архитектуре:** Task Router (Phase 1 roadmap), калибровка по доменам, Decision/Outcome tracking. Остальное — расширение существующей системы.

---

## Как физически подключается каждый LLM

### 1. VSCode Extension → MCP Server (stdio)

VSCode extension **запускает MCP Server как дочерний процесс**. Никакого отдельного сервера поднимать не нужно.

```typescript
// membriaMCPClient.ts — что реально происходит:
this.process = spawn('python3', ['/Users/.../start_mcp_server.py'], {
    env: {
        PYTHONPATH: '/Users/.../membria-cli/src',
        VIRTUAL_ENV: '/Users/.../membria-cli/.venv'
    }
});

// Общение через stdin/stdout:
this.process.stdin.write(JSON.stringify({
    jsonrpc: '2.0',
    id: 1,
    method: 'tools/call',
    params: { name: 'membria.capture_decision', arguments: {...} }
}) + '\n');

// Ответ приходит через stdout:
this.process.stdout.on('data', (data) => {
    const response = JSON.parse(data);  // { result: { decision_id: "dec_123" } }
});
```

**Итого:** VSCode → `spawn python3` → JSON-RPC через stdin/stdout → FalkorDB

---

### 2. Claude Code → MCP Server (MCP Protocol / stdio)

Claude Code использует **стандартный MCP Protocol**. Настройка в `.claude/claude.json`:

```json
{
  "mcpServers": {
    "membria": {
      "command": "python3",
      "args": ["src/membria/start_mcp_server.py"],
      "cwd": "/Users/.../membria-cli",
      "env": {
        "PYTHONPATH": "src",
        "FALKORDB_HOST": "192.168.0.105"
      }
    }
  }
}
```

**Что происходит:**
1. Claude Code при старте видит конфигурацию `mcpServers`
2. Запускает `python3 start_mcp_server.py` как subprocess
3. Общается по протоколу MCP (JSON-RPC 2.0 через stdio)
4. Claude видит 7 доступных tools и может вызывать их

**Итого:** Claude Code → читает claude.json → `spawn python3` → MCP stdio → FalkorDB

---

### 3. OpenAI Codex / GPT → через adapter (HTTP)

OpenAI не поддерживает MCP. Используется **обёртка** `openai_adapter.py`:

```python
# openai_adapter.py — оборачивает Membria tools в формат OpenAI Function Calling
functions = [
    {
        "name": "membria_capture_decision",
        "description": "Capture a developer decision",
        "parameters": {
            "type": "object",
            "properties": {
                "statement": {"type": "string"},
                "alternatives": {"type": "array", "items": {"type": "string"}},
                "confidence": {"type": "number"}
            }
        }
    },
    # ... остальные 6 tools
]

# Когда OpenAI вызывает function_call:
response = openai.ChatCompletion.create(
    model="gpt-4",
    messages=messages,
    functions=functions,
    function_call="auto"
)

# Adapter перехватывает function_call и вызывает Membria напрямую:
if response.function_call.name == "membria_capture_decision":
    result = membria_client.capture_decision(**args)
```

**Итого:** OpenAI API → function_call → adapter.py → HTTP к MCP daemon → FalkorDB

---

### 4. KiloCode / GLM-5 → потребует новый adapter

KiloCode с GLM-5 потребует **свой adapter** по модели OpenAI:

```python
# kilocode_adapter.py (концепт)
class KiloCodeAdapter:
    def __init__(self, membria_client):
        self.client = membria_client
    
    def wrap_tools_for_glm5(self):
        """Преобразовать 7 Membria tools в формат GLM-5 function calling"""
        # GLM-5 использует похожий на OpenAI формат
        return [...]
    
    def handle_function_call(self, call):
        """Перехватить function_call от GLM-5 и вызвать Membria"""
        tool_name = call['name']
        return getattr(self.client, tool_name)(**call['arguments'])
```

---

### Сводная таблица подключений

```
Способ подключения     Кто запускает        Протокол         Порт
────────────────────   ──────────────────   ──────────────   ─────
VSCode Extension       Extension (spawn)    JSON-RPC/stdio   —
Claude Code            Claude (spawn)       MCP/stdio        —
OpenAI Codex           Adapter (HTTP)       HTTP REST        3117
KiloCode/GLM-5         Adapter (HTTP)       HTTP REST        3117
membria CLI            CLI (direct)         Python import    —
```

---

## Зачем нужен CLI (`membria`)?

CLI — это **пульт управления** системой Membria. Он НЕ является посредником для AI-интеграций, а служит для администрирования.

### Что делает CLI

```
membria CLI
├── Lifecycle (жизненный цикл)
│   ├── membria init              # Инициализация проекта
│   ├── membria doctor            # Диагностика: всё ли работает?
│   └── membria daemon            # Управление MCP daemon
│       ├── start [--port 3117]   # Запустить
│       ├── stop                  # Остановить
│       ├── status                # Проверить статус
│       └── logs [-f]             # Посмотреть логи
│
├── Configuration (настройки)
│   └── membria config            # Управление конфигурацией
│       ├── get <key>
│       ├── set <key> <value>
│       └── list
│
├── Graph Operations (работа с графом)
│   ├── membria decisions         # CRUD решений
│   ├── membria engrams           # Управление engrams
│   ├── membria outcome           # Запись результатов
│   ├── membria calibration       # Калибровка команды
│   ├── membria antipattern       # Антипаттерны
│   └── membria graph             # Граф-агенты
│
├── Analysis (анализ)
│   ├── membria stats             # Статистика
│   ├── membria extractor         # Decision Extractor
│   └── membria safety            # Bias detection
│
└── Infrastructure (инфраструктура)
    ├── membria db                # Миграции БД
    └── membria webhook           # GitHub webhooks
```

### Аналогия

| Компонент | Аналогия |
|-----------|----------|
| **VSCode Extension** | Руль машины (управление в процессе работы) |
| **Claude / Codex** | Пассажиры, помогающие навигировать |
| **CLI** | Гаражный компьютер (диагностика, настройка, обслуживание) |
| **MCP Server** | Двигатель (все используют, никто не трогает напрямую) |
| **FalkorDB** | Память (карты, история поездок, настройки) |

### Примеры использования CLI

```bash
# Утро: проверить что всё работает
membria doctor

# Запустить daemon перед работой
membria daemon start

# Посмотреть решения за неделю
membria decisions list --since "7 days ago"

# Проверить калибровку по auth-задачам
membria calibration show --domain auth

# Мигрировать схему после обновления
membria db migrate

# Посмотреть антипаттерны
membria antipattern list

# Проверить bias detection
membria safety check "Use custom JWT"
```

### Когда CLI НЕ нужен

```
❌ Для работы с Claude — Claude подключается напрямую через claude.json
❌ Для работы в VSCode — Extension запускает MCP сам
❌ Для capture decisions — делается через tools в AI или VSCode

✅ CLI нужен для: init, daemon management, diagnostics, 
   миграции БД, bulk-операции с графом, отладка
```
