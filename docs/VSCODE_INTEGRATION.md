# VSCode Extension — Полная Документация

Membria Decision Intelligence extension для VSCode. Даёт доступ к decision intelligence прямо из редактора: capture решений, валидация планов, калибровка команды, inline-предупреждения.

**Время чтения:** ~20 минут

---

## Membria SuperAgent — Общая Картина

Membria SuperAgent — это система, в которой **три независимые точки входа** подключаются к одному и тому же бэкенду (MCP Server → FalkorDB). Они не зависят друг от друга:

```
┌──────────────────┐                          ┌──────────────────┐
│  1. VSCode Ext.  │──stdio (JSON-RPC)──────→│                  │
│  (TypeScript)    │  spawn python process    │                  │
└──────────────────┘                          │                  │
                                              │   MCP Server     │
┌──────────────────┐                          │   (Python)       │───→ FalkorDB
│  2. Claude Code  │──stdio (MCP Protocol)──→│                  │     (граф-БД)
│  (.claude.json)  │  настроен в claude.json  │                  │
└──────────────────┘                          │                  │
                                              │                  │
┌──────────────────┐                          │                  │
│  3. OpenAI Codex │──HTTP (function call)──→│                  │
│  (adapter.py)    │  openai_adapter обёртка  │                  │
└──────────────────┘                          └──────────────────┘
```

### Как подключается каждый компонент

| Компонент | Протокол | Как подключается | Зависит от CLI? |
|-----------|----------|------------------|:---------------:|
| **VSCode Extension** | JSON-RPC 2.0 (stdio) | Запускает MCP Server как дочерний процесс прямо из extension | Нет |
| **Claude Code** | MCP Protocol (stdio) | Настройка в `.claude/claude.json`, Claude сам запускает MCP Server | Нет |
| **OpenAI Codex** | HTTP (function calling) | `openai_adapter.py` оборачивает 7 Membria tools в формат OpenAI | Через CLI |

### Ключевые принципы

1. **Независимость** — каждая интеграция работает автономно. Можно использовать только Claude, только VSCode, или все три одновременно.
2. **Один граф** — все три пишут и читают из одного FalkorDB. Решение, захваченное через Claude, видно в VSCode sidebar.
3. **7 общих tools** — `capture_decision`, `record_outcome`, `get_calibration`, `get_decision_context`, `get_plan_context`, `validate_plan`, `record_plan` — доступны через любую интеграцию.
4. **MCP Server — единая точка** — все дороги ведут через один и тот же Python MCP-сервер (`start_mcp_server.py`).

> **Примечание:** CLI (`membria` команда) используется для управления системой (init, daemon, doctor, config), но НЕ является обязательным посредником для AI-интеграций. Claude и VSCode обходят CLI и общаются напрямую с MCP Server.

---

## Обзор Архитектуры

```
┌──────────────────────────────────────────┐
│            VSCode Extension              │
│                                          │
│  extension.ts (точка входа)              │
│  ├── MembriaMCPClient (JSON-RPC stdio)   │
│  ├── 4 TreeDataProvider (sidebar views)  │
│  ├── HoverProvider (контекст по наведению)│
│  ├── DecorationProvider (inline-метки)   │
│  └── 9 команд + 5 webview-панелей       │
│                                          │
│         ↕ JSON-RPC 2.0 (stdio)           │
│                                          │
│  Python MCP Server (start_mcp_server.py) │
│         ↕ Cypher queries                 │
│  FalkorDB (192.168.0.105:6379)           │
└──────────────────────────────────────────┘
```

### Два клиента коммуникации

Extension содержит **два клиента** для связи с бэкендом:

| Файл | Протокол | Порт | Используется |
|------|----------|------|:------------:|
| `membriaMCPClient.ts` | JSON-RPC 2.0 / stdio | — (stdin/stdout) | ✅ Да |
| `membriaClient.ts` | HTTP REST / axios | 6379 (или 3117) | Резерв |

**Активный клиент** — `MembriaMCPClient`. Он запускает Python MCP-сервер как дочерний процесс и общается через stdin/stdout по протоколу JSON-RPC 2.0.

---

## Установка и Запуск

### Prerequisites
- VSCode ≥ 1.85.0
- Node.js 14+
- Python 3.8+ (для MCP Server)
- FalkorDB сервер (запущен и доступен)

### Шаг 1: Установить зависимости

```bash
cd ~/membria-cli/vscode-extension
npm install
```

### Шаг 2: Собрать extension

```bash
npm run compile
```

### Шаг 3: Запустить в режиме отладки

В VSCode:
1. Открыть папку `vscode-extension` → `code .`
2. Нажать `F5` (Run → Start Debugging)
3. Откроется новое окно VSCode с загруженным extension
4. В новом окне: `Cmd+Shift+P` → набрать «Membria»

### Шаг 4: Упаковка для распространения

```bash
npm install -g @vscode/vsce
npm run vsce-package
# → Создаёт .vsix файл
```

Установить `.vsix`:
```bash
code --install-extension membria-1.0.0.vsix
```

---

## Конфигурация

Добавить в `.vscode/settings.json`:

```json
{
  "membria.enabled": true,
  "membria.serverHost": "localhost",
  "membria.serverPort": 3117,
  "membria.showContextOnHover": true,
  "membria.showWarningsInline": true,
  "membria.planModeEnabled": true,
  "membria.autoCapture": false
}
```

### Описание настроек

| Настройка | Тип | По умолчанию | Описание |
|-----------|-----|:------------:|----------|
| `membria.enabled` | boolean | `true` | Включить/выключить extension |
| `membria.serverHost` | string | `localhost` | Хост MCP сервера |
| `membria.serverPort` | integer | `3117` | Порт HTTP сервера daemon |
| `membria.showContextOnHover` | boolean | `true` | Показывать контекст при наведении |
| `membria.showWarningsInline` | boolean | `true` | Показывать inline-предупреждения |
| `membria.planModeEnabled` | boolean | `true` | Включить Plan Mode (PRE/MID/POST) |
| `membria.autoCapture` | boolean | `false` | Автоматический capture решений из комментариев |

---

## 9 Команд

### Горячие клавиши

Все команды используют префикс **`Cmd+Shift+M`** (macOS) / **`Ctrl+Shift+M`** (Windows/Linux):

| Клавиши | Команда | Описание |
|---------|---------|----------|
| `Cmd+Shift+M D` | Capture Decision | Записать решение |
| `Cmd+Shift+M C` | Get Context | Получить контекст решения |
| `Cmd+Shift+M V` | Validate Plan | Проверить план |
| `Cmd+Shift+M P` | Toggle Panel | Открыть/скрыть sidebar |

### Полный список команд

#### 1. `membria.captureDecision` — Записать Решение

Запрашивает три поля через диалоги ввода:
- **Statement** — формулировка решения
- **Alternatives** — альтернативы (через запятую)
- **Confidence** — уверенность (0–1)

```
Пример:
  Statement:    "Использовать PostgreSQL для user database"
  Alternatives: "MongoDB, MySQL, DynamoDB"
  Confidence:   0.8

→ Записывает Decision в граф через MCP
→ Обновляет sidebar "Decisions"
```

#### 2. `membria.getContext` — Получить Контекст

Запрашивает формулировку решения, открывает **webview-панель** сбоку с:
- Историей похожих решений
- Данными калибровки
- Предупреждениями из Negative Knowledge

#### 3. `membria.validatePlan` — Проверить План

Запрашивает шаги плана, проверяет их через MCP-сервер против:
- Negative Knowledge (известные провалы)
- AntiPatterns (частые ошибки)
- Past Failures (история команды)
- Calibration (разрыв уверенности)

Результат: webview-панель с количеством шагов, количеством предупреждений и статусом `can_proceed`.

#### 4. `membria.showPlans` — Показать Все Планы

Открывает webview-панель с таблицей всех планов: ID, домен, количество шагов, статус.

#### 5. `membria.showSkills` — Показать Skills

Открывает webview-панель со списком навыков: ID, домен, quality score, success rate.

#### 6. `membria.generateSkill` — Сгенерировать Skill

Предлагает выбрать домен из списка:
`database`, `auth`, `api`, `cache`, `messaging`, `storage`

Генерирует skill из накопленных outcomes и паттернов.

#### 7. `membria.togglePanel` — Открыть/Скрыть Sidebar

Переключает видимость панели Membria в Activity Bar.

#### 8. `membria.recordOutcome` — Записать Результат

Запрашивает:
- **Decision ID** (например, `dec_abc123`)
- **Status**: `success`, `failure`, или `partial`

Записывает outcome, обновляет дерево решений и калибровку.

#### 9. `membria.viewCalibration` — Калибровка Команды

Открывает webview-панель с:
- Success Rate
- Confidence Gap (разрыв между уверенностью и реальным успехом)
- Sample Size
- Примечания

---

## 4 Sidebar Views

Extension добавляет иконку **Membria** в Activity Bar (левая панель VSCode). Внутри — 4 вида:

### 📌 Decisions — История Решений

Дерево с 4 категориями:
- **Recent Decisions** — последние решения
- **Success Patterns** — успешные подходы (✅ зелёная иконка)
- **Failed Approaches** — неудачные подходы (❌ красная иконка)
- **In Progress** — решения в процессе (⏳ жёлтая иконка)

Кликни на решение → откроется детальная информация.

### 📊 Team Calibration — Калибровка

Группировка по доменам: `database`, `auth`, `api`, `cache`, `messaging`. Для каждого:
- **Success Rate** — процент успехов
- **Sample Size** — количество решений
- **Confidence Gap** — переоценка/недооценка уверенности
- **Trend** — динамика (Improving / Stable / Declining)

### ⭐ Skills — Навыки

Отображает skills с метриками:
- **Quality Score** — `success_rate × √sample_size / √n`
- **Success Rate** — эмпирический процент успехов
- **Зоны** (3 уровня):
  - 🟢 **Green Zone** (>75%) — Использовать уверенно
  - 🟡 **Yellow Zone** (50–75%) — Применять осторожно
  - 🔴 **Red Zone** (<50%) — Избегать

### 📋 Plans — Планы

Группировка по статусу:
- **Completed** — завершённые планы
- **In Progress** — в процессе
- **Pending** — ожидающие

Каждый план раскрывается в список шагов.

---

## Inline Функции

### Hover Context — Контекст по Наведению

При наведении курсора на код, **HoverProvider** запрашивает контекст у MCP-сервера и показывает:

```
┌──────────────────────────────┐
│ 📋 Membria Context           │
│                              │
│ Statement: Use Redis cache   │
│                              │
│ Calibration:                 │
│ - Success Rate: 82.5%        │
│ - Confidence Gap: +8.2%      │
│ - Sample Size: 24            │
│                              │
│ Recent Outcomes:             │
│ - success (0.9)              │
│ - partial (0.6)              │
│                              │
│ ⚠️ Warnings:                 │
│ - Custom cache often removed │
└──────────────────────────────┘
```

**Поддерживаемые языки:** Python, TypeScript, JavaScript

### Inline Decorations — Визуальные Индикаторы

**DecorationProvider** сканирует код и подсвечивает строки, содержащие «решения»:

| Цвет | Значение | Пример |
|------|----------|--------|
| 🔴 Красный | Предупреждение (risk) | «custom auth» — 89% провалов |
| 🟢 Зелёный | Уверенный паттерн | Success rate > 80% |
| 🔵 Синий | Информация (calibration gap > 10%) | «Команда переоценивает на +17%» |

**Паттерны обнаружения** (10 regex):
- `use \w+`, `implement \w+`, `adopt \w+`, `choose \w+`, `decide to`
- `should \w+`, `cache`, `database`, `auth`, `api`

Декорации обновляются при смене файла и при изменении текста.

---

## Структура Файлов

```
vscode-extension/
├── package.json                    # Манифест: 9 команд, 4 views, конфигурация
├── tsconfig.json                   # TypeScript: ES2020, strict mode
├── build.sh                        # Скрипт сборки
├── src/
│   ├── extension.ts                # Точка входа (activate/deactivate)
│   ├── membriaClient.ts            # HTTP-клиент (axios, резервный)
│   ├── membriaMCPClient.ts         # MCP-клиент (JSON-RPC, основной)
│   ├── test.ts                     # 10 интеграционных тестов
│   └── providers/
│       ├── decisionTreeProvider.ts  # Sidebar: Decisions
│       ├── calibrationProvider.ts   # Sidebar: Team Calibration
│       ├── skillsProvider.ts        # Sidebar: Skills
│       ├── plansProvider.ts         # Sidebar: Plans
│       ├── hoverProvider.ts         # Hover контекст (Py/TS/JS)
│       └── decorationProvider.ts    # Inline подсветка
├── media/                          # Иконки (пока пусто)
├── README.md                       # Быстрый юзер-гайд
├── DEVELOPMENT.md                  # Гайд для разработчиков
├── INTEGRATION_GUIDE.md            # Полный workflow интеграций
├── COMPLETION_STATUS.md            # Чеклист готовности
└── SETUP_CHECKLIST.md              # Чеклист установки
```

---

## Потоки Данных

### Capture Decision → Граф

```
Пользователь: Cmd+Shift+M D
    ↓
extension.ts: показывает 3 InputBox
    ↓
MembriaMCPClient.captureDecision(statement, alternatives, 0.8)
    ↓
JSON-RPC → stdin MCP Server:
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "membria.capture_decision",
    "arguments": {
      "statement": "Использовать PostgreSQL",
      "alternatives": ["MySQL", "MongoDB"],
      "confidence": 0.8,
      "context": { "module": "vscode" }
    }
  }
}
    ↓
MCP Server → FalkorDB: CREATE (d:Decision {...})
    ↓
Ответ → stdout → JSON.parse → resolve Promise
    ↓
decisionTreeProvider.refresh() → sidebar обновлён
```

### Validate Plan → Предупреждения

```
Пользователь: Cmd+Shift+M V
    ↓
Ввод шагов плана
    ↓
MembriaMCPClient.validatePlan(steps)
    ↓
MCP Server:
  1. Проверяет шаги vs NegativeKnowledge
  2. Проверяет vs AntiPatterns
  3. Смотрит Past Failures
  4. Считает Calibration Gap
    ↓
Возвращает { total_steps, warnings, high_severity, can_proceed }
    ↓
Если high_severity > 0 → showWarningMessage
Иначе → showInformationMessage "Plan looks good!"
    ↓
Открывает webview-панель с результатами
```

---

## Разработка

### Доступные npm scripts

```bash
npm run compile        # Собрать TypeScript → out/
npm run watch          # Автосборка при сохранении
npm test               # Запустить 10 тестов
npm run lint           # ESLint
npm run vsce-package   # Создать .vsix
npm run publish        # Опубликовать в Marketplace
npm run clean          # Удалить out/, node_modules/, *.vsix
```

### Добавить новую команду

1. Зарегистрировать в `extension.ts` → `registerCommands()`
2. Добавить в `package.json` → `contributes.commands`
3. (Опционально) Добавить keybinding в `contributes.keybindings`
4. (Опционально) Добавить в `contributes.menus.commandPalette`

### Добавить новый sidebar view

1. Создать `src/providers/myProvider.ts` — имплементация `TreeDataProvider`
2. Зарегистрировать в `extension.ts`: `vscode.window.registerTreeDataProvider()`
3. Добавить в `package.json` → `contributes.views.membria-explorer`

---

## Зависимости

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| `axios` | ^1.6.2 | HTTP-клиент (резервный протокол) |
| `@types/vscode` | ^1.85.0 | Типы VSCode API |
| `typescript` | ^5.3.3 | Компилятор |
| `eslint` | ^8.55.0 | Линтер |
| `vsce` | ^2.15.0 | Упаковка extension |

---

## Troubleshooting

### MCP Server не подключается
```bash
# Проверить что MCP сервер может запуститься:
cd ~/membria-cli
source .venv/bin/activate
python src/membria/start_mcp_server.py
```

### Hover контекст не появляется
- Убедиться что `membria.showContextOnHover: true` в settings
- Файл должен быть `.py`, `.ts` или `.js`
- MCP сервер должен быть запущен

### Sidebar пустой
- Нажать `Cmd+Shift+M P` для открытия панели Membria
- Проверить Output → «Membria» канал в VSCode
- Перезагрузить окно: `Cmd+Shift+P` → «Reload Window»

### Extension не активируется
- Проверить что `npm run compile` прошёл без ошибок
- Открыть Developer Tools: `Help → Toggle Developer Tools`
- Проверить консоль на ошибки

---

## Текущие Ограничения

1. **Иконки отсутствуют** — директория `media/` пуста; extension работает без иконок, но Activity Bar показывает дефолтную иконку
2. **Mock-данные** — sidebar providers возвращают захардкоженные примеры; реальные данные загружаются при подключённом MCP сервере
3. **Нет debounce** в DecorationProvider — при каждом изменении текста переcканирует весь файл
4. **Один процесс MCP** — при перезапуске extension, старый процесс MCP может остаться висеть

---

## Связанные Документы

- [Claude Integration](CLAUDE_INTEGRATION.md) — интеграция с Claude Code через MCP
- [OpenAI Integration](OPENAI_INTEGRATION.md) — интеграция с OpenAI Codex
- [macOS Setup Guide](MACOS_SETUP_GUIDE.md) — установка на Mac
- [Quickstart](QUICKSTART.md) — быстрый старт
