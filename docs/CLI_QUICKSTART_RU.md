# Membria CLI: Первый запуск за 60 секунд 🚀

Самый простой путь от установки до первого решения, принятого с помощью ИИ-памяти.

---

### Шаг 1: Настройка (Onboarding)

Запустите интерактивный помощник, чтобы подключить ключи и выбрать модель:

```bash
membria onboarding
```

_Совет: выберите пресет "Full Power" для лучшего опыта._

### Шаг 2: Интерактивная сессия

Зайдите в оболочку и попросите агентов составить план:

```bash
membria shell
```

Внутри шелла введите:

```bash
/plan "Добавить систему кэширования в проект на FastAPI"
```

_Membria выберет Архитектора, Программиста и Ревьюера для решения задачи._

### Шаг 3: Автоматизация и Визуализация

Включите фиксацию решений при каждом коммите и посмотрите на свой граф знаний:

```bash
# Включаем Git-хуки
membria hooks install

# Смотрим на карту решений в браузере
membria dashboard
```

---

### Основные команды (Cheat Sheet):

- `/plan [задача]` — запустить мульти-агентную оркестрацию.
- `/status` — проверить состояние агентов и провайдеров.
- `/agents` — посмотреть статистику репутации вашей команды.
- `exit` — выход из шелла.

**Готово!** Вы настроили систему ИИ-делегирования с памятью решений.
