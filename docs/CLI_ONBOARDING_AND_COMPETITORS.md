# Membria CLI: First-Run Onboarding и Конкурентный Анализ

---

## Конкуренты: Как устроены их CLI

### Сводная таблица

| Инструмент | Тип | Multi-Model | Onboarding | Ближайший к Membria? |
|------------|-----|:-----------:|-----------|:--------------------:|
| **Goose** (Block) | CLI + Desktop | ✅ Lead/Worker (2 модели) | CLI → config → выбор провайдера | **🏆 Ближайший** |
| **Aider** | CLI | ✅ `/model` — смена на лету | `.env` с ключами → `aider` → REPL | Близкий |
| **Claude Code** | CLI | ❌ Только Claude | `npm install` → `claude login` (browser OAuth) → `/init` | Эталон UX |
| **CrewAI** | Framework | ✅ YAML agents | `crewai create crew` → agents.yaml → tasks.yaml | Ролевая модель |
| **Roo Code** | VSCode Ext | ✅ 20+ провайдеров | Settings → выбор провайдера → ключ | Режимы Code/Architect/Ask |
| **KiloCode** | VSCode Ext | ✅ Fork Roo | Аналогично Roo | Superset Cline+Roo |

### 🏆 Goose — Ближайший Конкурент

Goose от Block — **единственный инструмент с двухмодельной оркестрацией** (Lead/Worker):

```
Lead Model (Claude Sonnet):      Worker Model (GPT-4.1-mini):
  → Reasoning                       → Execution
  → Planning                        → Code generation
  → Complex decisions                → Simple tasks
  
  Если Worker не справляется → Goose автоматически
  возвращает задачу Lead модели
```

**Что взять от Goose:**
- Lead/Worker → расширить до N ролей (Architect/Senior/Junior/Reviewer)
- Автоматический fallback (Worker → Lead) при сложности
- 25+ провайдеров через MCP integration

**Чего нет у Goose (есть у Membria):**
- ❌ Нет Decision Memory
- ❌ Нет калибровки per-model
- ❌ Нет NegativeKnowledge
- ❌ Только 2 модели (Lead/Worker), не N

### Aider — Эталон Multi-Model CLI

```bash
# .env — все ключи в одном месте:
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

$ aider                          # Запускает REPL
$ aider --model deepseek/...     # Или с конкретной моделью

# В сессии:
> /model claude-3-5-sonnet       # Смена модели на лету
> /tokens                        # Стоимость текущей сессии
> /add file.py                   # Добавить файл в контекст
```

**Что взять от Aider:**
- `.env` для ключей (просто и понятно)
- `/model` для быстрой смены
- `/tokens` для отслеживания стоимости

### Claude Code — Эталон Первого Запуска

```bash
$ npm install -g @anthropic-ai/claude-code
$ claude                         # Первый запуск

# Автоматически:
# 1. Открывает браузер для OAuth login
# 2. Сканирует проект (CLAUDE.md)
# 3. Показывает возможности
# 4. Предлагает /init для анализа проекта

> /init                          # Анализ проекта, создаёт CLAUDE.md
> /permissions                   # Настройка прав доступа
```

**Что взять от Claude Code:**
- `/init` для первичного анализа проекта
- `MEMBRIA.md` (аналог CLAUDE.md) для персистентного контекста
- Красивый, минималистичный UX

### CrewAI — Эталон Ролевой Модели

```yaml
# agents.yaml
researcher:
  role: Senior Researcher
  goal: Find accurate information
  backstory: Expert analyst with 20 years experience
  llm: anthropic/claude-sonnet

writer:
  role: Technical Writer
  goal: Create clear documentation
  backstory: Technical communicator
  llm: openai/gpt-4.1
```

**Что взять от CrewAI:**
- YAML-конфигурация агентов (роль + цель + backstory + модель)
- Hierarchical mode (Manager Agent → Sub-agents)

---

## Membria CLI: First-Run Flow

### Шаг 0: Установка

```bash
pip install membria
# или
pipx install membria
```

### Шаг 1: Первый запуск `membria`

```
$ membria

 ███╗   ███╗███████╗███╗   ███╗██████╗ ██████╗ ██╗ █████╗ 
 ████╗ ████║██╔════╝████╗ ████║██╔══██╗██╔══██╗██║██╔══██╗
 ██╔████╔██║█████╗  ██╔████╔██║██████╔╝██████╔╝██║███████║
 ██║╚██╔╝██║██╔══╝  ██║╚██╔╝██║██╔══██╗██╔══██╗██║██╔══██║
 ██║ ╚═╝ ██║███████╗██║ ╚═╝ ██║██████╔╝██║  ██║██║██║  ██║
 ╚═╝     ╚═╝╚══════╝╚═╝     ╚═╝╚═════╝ ╚═╝  ╚═╝╚═╝╚═╝  ╚═╝
                                                    v1.0.0
              Decision Intelligence SuperAgent

 👋 Привет! Похоже, это первый запуск.
    Давайте настроим Membria за 3 шага.
    
 ─────────────────────────────────────────────── Step 1/3 ──
 
 🔌 ПОДКЛЮЧЕНИЕ ПРОВАЙДЕРОВ
 
 Membria работает с любыми code LLM. Подключите хотя бы
 одного провайдера для начала работы.

 Доступные провайдеры:
 
   [1] Anthropic     (Claude Sonnet, Haiku, Opus)
   [2] OpenAI        (GPT-4.1, o3, Codex)
   [3] KiloCode      (DeepSeek, Qwen, GLM, Mistral, Codestral)
   [4] Ollama        (Локальные модели — бесплатно)
   [5] OpenRouter    (200+ моделей через один API key)
   [6] Другой        (Любой OpenAI-совместимый)
   
 Выберите провайдеров (через запятую): ▸ 1, 3, 4

 ── Anthropic ──────────────────────────────────────────────
 API Key (или env: ANTHROPIC_API_KEY): ▸ sk-ant-...
 ✅ Подключено! Доступные модели:
    • claude-sonnet-4   ($3/$15 per 1M tokens)
    • claude-haiku-3.5  ($0.25/$1.25)
    • claude-opus-4     ($15/$75)

 ── KiloCode ───────────────────────────────────────────────
 API Key (или env: KILOCODE_API_KEY): ▸ kc-...
 ✅ Подключено! Доступные модели:
    • deepseek-v3       ($0.14/$0.28)
    • deepseek-r1       ($0.55/$2.19)
    • qwen-2.5-coder    ($0.50/$1.00)
    • glm-5             ($0.50/$1.00)
    • codestral         ($0.30/$0.90)
    • mistral-large     ($2/$6)

 ── Ollama ─────────────────────────────────────────────────
 Endpoint: ▸ http://localhost:11434
 ✅ Подключено! Локальные модели:
    • llama-3.3-70b     (бесплатно)
    • deepseek-coder-33b (бесплатно)
```

### Шаг 2: Собрать команду

```
 ─────────────────────────────────────────────── Step 2/3 ──

 🤖 СБОРКА КОМАНДЫ

 Membria организует модели в команду разработки.
 Назначьте модели на роли:

 Доступные роли:
   🏗️  Architect   — проектирование, решения, документация
   ⚡  Senior Dev  — сложная реализация, рефакторинг
   💻  Junior Dev  — boilerplate, CRUD, простые задачи
   🔍  Reviewer    — code review, тесты, edge cases
   🐛  Debugger    — поиск и исправление багов

 Или выберите готовый пресет:
 
   [1] 🏆 Полная команда   Sonnet + GPT-4.1 + DeepSeek + Qwen
   [2] 💰 Экономная        DeepSeek R1 + DeepSeek V3 + Llama (local)
   [3] ⚡ Быстрая          Haiku + GPT-4.1-mini + DeepSeek V3
   [4] 🎯 Настроить вручную

 Выберите: ▸ 1

 ✅ Команда собрана:
 
 ┌──────────────────────────────────────────────────────┐
 │  🏗️ Architect    claude-sonnet-4     Anthropic       │
 │  ⚡ Senior Dev   gpt-4.1            OpenAI (через KiloCode нет) │
 │  💻 Junior Dev   deepseek-v3        KiloCode        │
 │  🔍 Reviewer     qwen-2.5-coder    KiloCode        │
 │  🐛 Debugger     deepseek-r1        KiloCode  [on demand] │
 └──────────────────────────────────────────────────────┘

 Примерная стоимость сессии (1 час): ~$0.50 - $2.00

 [y] Сохранить  [e] Редактировать  [p] Другой пресет
 ▸ y
```

### Шаг 3: Инициализация проекта

```
 ─────────────────────────────────────────────── Step 3/3 ──

 📁 ИНИЦИАЛИЗАЦИЯ ПРОЕКТА

 Текущая директория: ~/myapp
 
 🔍 Сканирование проекта...
 
   Язык:     TypeScript / Node.js
   Фреймворк: Express.js
   Тесты:    Jest (47 файлов)
   Git:      ✅ (последний коммит: 2ч назад)
   Размер:   234 файла, 18,400 строк
   
 📊 Подключение к FalkorDB...
   ✅ FalkorDB (192.168.0.105:6379) — connected
   📈 Existing decisions: 47 | Outcomes: 31

 📝 Создаю MEMBRIA.md (аналог CLAUDE.md)...
   ✅ ~/myapp/MEMBRIA.md — создан
   
   Содержимое MEMBRIA.md:
   ┌──────────────────────────────────────────────────┐
   │ # Membria Project Context                       │
   │                                                  │
   │ ## Project: myapp                                │
   │ - Stack: TypeScript, Express.js, PostgreSQL      │
   │ - Test runner: Jest                              │  
   │ - Architecture: MVC + Service layer              │
   │                                                  │
   │ ## Team Preferences                              │
   │ - Prefer express-rate-limit over custom          │
   │ - Use SendGrid for email (proven in dec_023)     │
   │ - Avoid custom auth (failure rate 89%)           │
   │                                                  │
   │ ## Known Anti-patterns (auto-populated)          │
   │ - Custom JWT implementation → 89% failure        │
   │ - Manual rate limiting → removed 78% repos       │
   └──────────────────────────────────────────────────┘

 ──────────────────────────────────────────── Setup Done ──

 ✅ Membria готова к работе!
 
    Подключено: 3 провайдера, 11 моделей
    Команда:    5 агентов
    Проект:     ~/myapp (TypeScript)
    Graph:      47 решений, 31 outcome
    
 💡 Подсказки:
    • Просто напишите задачу на естественном языке
    • @architect, @senior, @junior — прямое обращение
    • /help — все команды
    • /agents — управление командой
    • /mode — переключение режима оркестрации
    
 membria ~/myapp ▸ 
```

---

## Повторные запуски

После первой настройки, запуск быстрый:

```
$ membria

 ███╗   ███╗███████╗███╗   ███╗██████╗ ██████╗ ██╗ █████╗ 
 ...                                               v1.0.0
              Decision Intelligence SuperAgent

 ┌─────────────────────────────────────────────────────┐
 │  🧠 Graph:     ✅ FalkorDB (47 decisions, 31 outcomes) │
 │  ⚡ Calibration: -8% overconfident (improving ↑)    │
 │                                                     │
 │  🤖 Team:                                           │
 │     🏗️ Architect  claude-sonnet-4     ✅ Ready       │
 │     ⚡ Senior     gpt-4.1            ✅ Ready       │
 │     💻 Junior     deepseek-v3        ✅ Ready       │
 │     🔍 Reviewer   qwen-2.5-coder    ✅ Ready       │
 │     🐛 Debugger   deepseek-r1        💤 On Demand   │
 │                                                     │
 │  📁 ~/myapp (TypeScript + Express)                  │
 │  🔥 Last session: 2h ago (12 decisions, 3 plans)    │
 │  💡 Tip: "Добавь кэширование для каталога"          │
 └─────────────────────────────────────────────────────┘

 membria ~/myapp ▸ 
```

---

## Workflow: Типовые Сценарии Работы

### Сценарий 1: Новая фича

```
membria ~/myapp ▸ Добавить систему уведомлений с email и push

# TeamLead (Membria) автоматически:
# 1. Проверяет граф → находит похожие решения
# 2. Показывает NegativeKnowledge
# 3. Направляет Architect

🧠 Context: SendGrid для email ✅ (dec_023), Custom email ❌ (dec_009)

🏗️ Architect (claude-sonnet-4) проектирует...
   [план + decision + estimate]

▸ y (принять план)

⚡ Senior + 💻 Junior работают параллельно...
   [progress bar]

🔍 Reviewer проверяет...
   [результаты ревью]

▸ y (применить)
✅ Done. 8 files created.
```

### Сценарий 2: Баг-фикс

```
membria ~/myapp ▸ В auth middleware возвращается 500 на expired tokens

# TeamLead определяет: debugging + auth domain
# Auth domain: calibration gap +12% → нужен Debugger

🐛 Debugger (deepseek-r1) активирован (reasoning mode)...

🐛 Root cause found:
   jwt.verify() throws on expired, but catch block
   only handles malformed tokens.
   
   Fix: Add specific handling for TokenExpiredError
   
   ┌─ DIFF ──────────────────────────────────────┐
   │ @@ -15,6 +15,8 @@                           │
   │  } catch(error) {                            │
   │ +  if (error instanceof TokenExpiredError) {  │
   │ +    return res.status(401).json({...})       │
   │ +  }                                         │
   │    return res.status(400).json({...})         │
   │  }                                           │
   └──────────────────────────────────────────────┘

▸ y
✅ Fix applied. Decision dec_050 recorded.
```

### Сценарий 3: Прямое обращение к агенту

```
membria ~/myapp ▸ @architect Объясни зачем здесь паттерн Strategy

🏗️ Claude (Architect):
   Паттерн Strategy использован в NotificationService для:
   1. Подменяемых каналов (Email, Push, InApp)
   2. Тестируемости — можно мокать каждый канал
   3. Open/Closed — новые каналы без модификации базы
   
   Это решение dec_023 от 15.01.2026, confidence 0.85,
   outcome: SUCCESS. Калибровка подтвердила правильность.

membria ~/myapp ▸ @junior Сгенерируй CRUD для заметок

💻 Junior (deepseek-v3) генерирует...
   Created: 
   • src/models/Note.ts
   • src/routes/notes.ts  
   • src/services/noteService.ts
   • tests/notes.test.ts
   
   [y] Применить  [d] Diff
```

### Сценарий 4: Смена команды на лету

```
membria ~/myapp ▸ /agents swap senior anthropic/claude-opus-4

✅ Senior Dev: gpt-4.1 → claude-opus-4
   ⚠️ Cost increase: ~$2/session → ~$10/session
   Calibration: new model, need 5+ decisions for data

membria ~/myapp ▸ /agents add tester kilocode/codestral --role reviewer

✅ Added Tester (codestral via KiloCode) as Reviewer
   Now 2 reviewers: qwen-2.5-coder + codestral
   Mode: both review in parallel (fan-out)
```

### Сценарий 5: Переключение режимов

```
membria ~/myapp ▸ /mode

 Current mode: pipeline
 
 Available modes:
   [1] pipeline     Architect → Implementer → Reviewer (current)
   [2] fanout       All agents on same task → pick best
   [3] specialist   Each agent gets own domain
   [4] solo <name>  Work with single agent
   [5] pair <a> <b> Two agents collaborating

 ▸ 4 architect

 ✅ Mode: solo (Architect only — claude-sonnet-4)
 All tasks go to Architect. No review.
 
membria ~/myapp (solo: 🏗️) ▸ 
```

---

## Хранение Конфигурации

```
~/.membria/
├── config.yaml           # Провайдеры, команда, оркестрация
├── credentials.env       # API ключи (или env vars)
├── teams/                # Сохранённые составы команд
│   ├── full.yaml         # Полная команда
│   ├── budget.yaml       # Экономная
│   └── fast.yaml         # Быстрая
├── sessions/             # История сессий
│   ├── 2026-02-14-myapp.log
│   └── ...
└── cache/                # Кэш моделей и ответов
```

Проектный контекст:
```
~/myapp/
├── MEMBRIA.md            # Контекст проекта (auto-generated)
├── .membria/
│   ├── team.yaml         # Override команды для проекта
│   └── rules.yaml        # Проектные правила и constraints
```

---

## Сравнение Onboarding Flow

| Шаг | Claude Code | Aider | Goose | **Membria** |
|-----|------------|-------|-------|-------------|
| Установка | npm install | pip install | curl/brew | pip install |
| Авторизация | Browser OAuth | .env файл | CLI config | CLI wizard |
| Выбор модели | Нет (Claude only) | `--model` флаг | CLI → provider | Интерактивный wizard |
| Multi-model | ❌ | `/model` на лету | Lead/Worker (2) | **Wizard → команда (N)** |
| Проект init | `/init` → CLAUDE.md | Auto-detect git | Auto-detect | **Scan + MEMBRIA.md + graph** |
| Готовность | ~2 мин | ~1 мин | ~3 мин | **~5 мин (первый раз)** |
| Повторный запуск | ~3 сек | ~2 сек | ~3 сек | **~3 сек** |
